/**
 * Classes
 */

let Cake = {};


// Old way before ECMA 6
Cake.prototype.bake = function(temp, minutes) {
  // Bake a cake at a particular temperature
  // for a number of minutes
}

// Like other Programming Languages (after ECMAScript 6)
class CakeClass {
  bake(temp, minutes) {

  }
}
